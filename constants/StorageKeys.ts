// AsyncStorage keys for persisting app state
export const STORAGE_KEYS = {
  MANUAL_DISTRICT_SELECTION: '@istanbuller:manual_district',
  LAST_AUTO_DETECTED_DISTRICT: '@istanbuller:last_auto_district',
} as const;
